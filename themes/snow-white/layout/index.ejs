---
layout: layout
---

<section class="max-w-3xl mx-auto py-10">
    <div class="post-preview-container space-y-4">
        <% page.posts.each(function(post){ %>
            <div class="post-preview">
                <div class="flex flex-row">
                    <div class="basis-1/4">
                        <div class="post-time text-sm text-gray-500">
                            <time datetime="<%= post.date %>"><%= post.date.format(config.date_format) %></time>
                        </div>
                    </div>
                    <div class="basis-3/4">
                        <div class="post-info text-gray-400">
                            <a href="<%- url_for(post.path) %>">
                                <h3>
                                    <%- (post.title || "Untitled").replace(/[<>&"]/g, function (c) {
                                        return {'<': '&lt;', '>': '&gt;', '&': '&amp;', '"': '&quot;'}[c];
                                    }) %>
                                </h3>
                            </a>

                            <p class="text-xs mt-2">
                                <% if (post.tags.length){ %>
                                    <span>/</span>
                                    <% post.tags.forEach(function(tag, i){ %>
                                        <a class="tag text-gray-500" href="<%= config.root %>tags/#<%= tag.name %>"
                                           title="<%= tag.name %>"><%= tag.name %></a>
                                        <% if (i < post.tags.length - 1) { %>
                                        <span>/</span>
                                        <% } %>
                                    <% }) %>
                                <% } %>
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
</section>

<%- partial('_partial/pagination', null, {cache: false}) %>