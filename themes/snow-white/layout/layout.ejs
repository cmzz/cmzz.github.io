<!DOCTYPE html>
<html lang=<%= config.language === 'zh' ? 'zh-CN' : config.language %> class="bg-gray-100">
<%- partial('_partial/head') %>

<body>

<div class="index-container py-2 md:py-8 px-2 md:px-0">
    <div class="main-content-wrapper max-w-3xl m-auto bg-white pt-8 pb-6 px-10">
        <%- partial('_partial/header') %>

        <% if(!is_current('/404')){%>
        <% } %>

        <main>
            <%- body %>
        </main>

        <%- partial('_partial/footer', null, {cache: false}) %>
    </div>
</div>

</body>
</html>